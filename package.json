{
  "name": "webease-root",
  "version": "1.0.0",
  "private": true,
  "description": "WebEase - AI-Powered Accessibility Browser Companion",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "backend": ".\\.venv\\Scripts\\python.exe -m uvicorn --app-dir backend main:app --reload --host 127.0.0.1 --port 8000"
  }
}
